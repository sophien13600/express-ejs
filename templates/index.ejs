<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page d'acceuil</title>
</head>
<body>
    <h1>Page d'accueil</h1>
    <p>
        Express.js + EJS
    </p>
    <p>
        Bonjour <%= nom %>
    </p>
    <p>

        Bonjour <%- nomImportant %>
    </p>
        <p>

        Bonjour <%= nomImportant %>
    </p>
    <h2>Structure de controle</h2>
    <h3>Structure conditionnelles</h3>
    <% if (isConnected) { %>
    <p>
        Vous êtes connecté
    </p>
    <%}else{ %>
        <p>Vous n'êtes pas connecté</p>
    <% } %>
    <h3>Structure itératives</h3>
    <ul>
        <% for ( const elt of nombres ) {%>
            <% if(( elt %2) ==0 ) {%>
        <li>
            <%= elt %> est pair
        </li>
        <%}else{%>
             <li>
            <%= elt %> est impair
        </li>
        <%}}%>
 
    </ul>
    <ul>
    <% nombres.forEach(elt => { %>
    <li> <%= elt %> <%= elt % 2 == 0 ? 'pair' : 'impair' %> </li>
    <% }) %>
  </ul>
  <ul>
    <% nombres.forEach(elt => { %>
    <li>
      <% let parity = elt % 2 == 0 ? 'pair' : 'impair' %>
      <%= `${elt} : ${parity}` %> </li>
    <% }) %>
  </ul>
  <%- include('footer.ejs', {year: 2025}) %>
</body>
</html>